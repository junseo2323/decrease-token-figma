async function main() {
  console.log('ℹ️  Figma Bridge requires Ollama at runtime. The MCP server will install/start Ollama and pull the default model automatically when it boots.');
  console.log('   You can also run `npm run setup` now to prepare Ollama ahead of time.');
}

main();
